# Мониторинг репликации PostgreSQL

Инструменты для мониторинга и тестирования репликации баз данных PostgreSQL.

## Описание

Эта папка содержит скрипты для мониторинга состояния репликации между PRIMARY (мастер) и STANDBY (слейв) серверами PostgreSQL.

## Файлы

### 1. `monitor_replication.py`
Консольный скрипт мониторинга репликации.

**Функции:**
- Проверяет подключение к PRIMARY и STANDBY серверам
- Отображает статус репликации
- Показывает метрики:
  - Количество активных реплик
  - Задержку репликации (lag) в байтах и секундах
  - Состояние WAL receiver
  - LSN позиции (Log Sequence Number)
- Поддерживает режим непрерывного мониторинга
- Предоставляет удобочитаемые отчеты

**Использование:**

```bash
# Однократная проверка
python backend/monitoring/monitor_replication.py --once

# Непрерывный мониторинг (каждые 10 секунд)
python backend/monitoring/monitor_replication.py
```

**Пример вывода:**
```
======================================================================
  ОТЧЁТ О РЕПЛИКАЦИИ - 2025-12-17 00:30:00
======================================================================

[PRIMARY SERVER] 127.0.0.1:5432
  Статус: OK
  Найдено 1 активных реплик

  Реплика #1:
    IP: 127.0.0.1
    Состояние: streaming
    Режим: async
    Отставание: 0 байт

[STANDBY SERVER] 127.0.0.1:5433
  Статус: OK
  Standby работает нормально
  Режим восстановления: ДА
  WAL Receiver: streaming
  Последний LSN: 0/5000000
  Последнее сообщение: 2025-12-17 00:29:59.123456+00
  Задержка репликации: 0.12 секунд
    ✓ Отличная синхронизация!

======================================================================
  ОБЩИЙ СТАТУС: OK
======================================================================
```

---

### 2. `web_monitor.py`
Веб-панель мониторинга на FastAPI.

**Функции:**
- Предоставляет веб-интерфейс для мониторинга
- Автоматически обновляется каждые 5 секунд
- Цветовая индикация состояния:
  - 🟢 Зеленый - всё в порядке (OK)
  - 🔴 Красный - ошибка (ERROR)
  - 🟡 Желтый - предупреждение (WARNING)
- Показывает те же метрики, что и консольный скрипт

**Использование:**

```bash
python backend/monitoring/web_monitor.py
```

Затем откройте браузер: `http://localhost:8081`

**Технологии:**
- FastAPI - веб-фреймворк
- HTML/CSS - интерфейс
- psycopg2 - подключение к PostgreSQL

---

### 3. `test_replication.py`
Скрипт для функционального тестирования репликации.

**Функции:**
- Создает тестовую таблицу `replication_test`
- Вставляет тестовую запись на PRIMARY
- Проверяет появление записи на STANDBY
- Возвращает код выхода:
  - `0` - тест пройден (репликация работает)
  - `1` - тест не пройден (проблема с репликацией)

**Использование:**

```bash
python backend/monitoring/test_replication.py
```

**Пример вывода:**
```
1. Вставка тестовых данных на PRIMARY...
   ✓ Вставлена запись с ID=44

2. Ожидание 2 секунды для репликации...

3. Проверка данных на STANDBY...
   ✓ УСПЕХ! Данные реплицировались корректно
   Найдено: Test at 2025-12-17 00:32:50.401256
```

---

## Конфигурация

Все скрипты используют следующие настройки подключения:

**PRIMARY сервер (мастер):**
- Host: `127.0.0.1`
- Port: `5432`
- User: `postgres`
- Password: `postgres`
- Database: `postgres`

**STANDBY сервер (слейв):**
- Host: `127.0.0.1`
- Port: `5433`
- User: `postgres`
- Password: `postgres`
- Database: `postgres`

Для изменения настроек отредактируйте переменные `PRIMARY_CONFIG` и `STANDBY_CONFIG` в каждом скрипте.

---

## Требования

### Python библиотеки:
```bash
pip install psycopg2-binary
pip install fastapi uvicorn  # только для web_monitor.py
```

### Версии:
- Python 3.8+
- PostgreSQL 12+

---

## Метрики репликации

Скрипты мониторят следующие показатели:

| Метрика | Описание | Откуда берется |
|---------|----------|----------------|
| **client_addr** | IP-адрес реплики | `pg_stat_replication.client_addr` |
| **state** | Состояние репликации | `pg_stat_replication.state` |
| **sent_lsn** | Отправленная позиция WAL | `pg_stat_replication.sent_lsn` |
| **replay_lsn** | Примененная позиция WAL | `pg_stat_replication.replay_lsn` |
| **lag_bytes** | Отставание в байтах | `pg_wal_lsn_diff(sent_lsn, replay_lsn)` |
| **sync_state** | Режим синхронизации | `pg_stat_replication.sync_state` |
| **is_recovery** | Режим восстановления | `pg_is_in_recovery()` |
| **wal_receiver** | Статус приемника WAL | `pg_stat_wal_receiver` |
| **lag_seconds** | Задержка в секундах | `now() - pg_last_xact_replay_timestamp()` |

---

## Интерпретация результатов

### Задержка репликации (lag_seconds):
- **< 1 сек** - ✓ Отличная синхронизация
- **1-5 сек** - ⚠ Небольшая задержка (нормально для асинхронной репликации)
- **> 5 сек** - ✗ Большая задержка (требует внимания)

### Состояния репликации (state):
- **streaming** - ✓ Активная передача данных (норма)
- **catchup** - ⚠ Реплика догоняет мастер (временное состояние)
- **startup** - ⚠ Инициализация репликации
- **backup** - ℹ️ Выполняется pg_basebackup
- **stopping** - ⚠ Остановка репликации

### Режим синхронизации (sync_state):
- **async** - Асинхронная репликация (используется по умолчанию)
- **sync** - Синхронная репликация (ждет подтверждения от standby)
- **potential** - Потенциальная синхронная реплика
- **quorum** - Участвует в кворуме синхронных реплик

---

## Устранение неполадок

### Ошибка: "Нет активных подключений репликации"
**Причины:**
- STANDBY сервер не запущен
- Неверная конфигурация репликации
- Проблемы с сетью

**Решение:**
1. Проверьте, запущен ли STANDBY: `pg_ctl status -D /path/to/standby/data`
2. Проверьте логи PostgreSQL
3. Проверьте настройки в `postgresql.conf` и `pg_hba.conf`

---

### Ошибка: "WAL receiver не активен"
**Причины:**
- STANDBY не подключается к PRIMARY
- Неверный пароль пользователя репликации
- Неверные настройки в `standby.signal` или `postgresql.auto.conf`

**Решение:**
1. Проверьте файл `standby.signal` в data-директории STANDBY
2. Проверьте параметр `primary_conninfo` в `postgresql.auto.conf`
3. Проверьте логи STANDBY сервера

---

### Большая задержка репликации
**Причины:**
- Высокая нагрузка на PRIMARY
- Медленная сеть
- Медленный диск на STANDBY
- Большой объем данных для репликации

**Решение:**
1. Проверьте нагрузку на серверы: `top`, `htop`
2. Проверьте сетевую задержку: `ping`
3. Проверьте производительность дисков: `iostat`
4. Рассмотрите увеличение `max_wal_senders` и `wal_keep_size`

---

## Автоматизация

### Запуск мониторинга как службы (systemd на Linux):

Создайте файл `/etc/systemd/system/pg-replication-monitor.service`:

```ini
[Unit]
Description=PostgreSQL Replication Monitor
After=network.target postgresql.service

[Service]
Type=simple
User=postgres
WorkingDirectory=/path/to/TRiZBD_UP
ExecStart=/usr/bin/python3 /path/to/TRiZBD_UP/backend/monitoring/monitor_replication.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable pg-replication-monitor
sudo systemctl start pg-replication-monitor
```

---

### Запуск через cron (периодическая проверка):

```bash
# Проверка каждые 5 минут, запись в лог
*/5 * * * * /usr/bin/python3 /path/to/TRiZBD_UP/backend/monitoring/monitor_replication.py --once >> /var/log/pg-replication-monitor.log 2>&1
```

---

## Дополнительные материалы

- Документация PostgreSQL: [High Availability, Load Balancing, and Replication](https://www.postgresql.org/docs/current/high-availability.html)
- Системные представления репликации: [pg_stat_replication](https://www.postgresql.org/docs/current/monitoring-stats.html#PG-STAT-REPLICATION-VIEW)
- Настройка репликации: см. `docs/05_obyasnenie/`

---

## Авторство

Скрипты созданы для учебного проекта TRiZBD_UP (Технологии разработки и защиты баз данных).

**Дата создания:** 2025-12-17
**Версия:** 1.0
